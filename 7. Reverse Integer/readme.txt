将输入的数倒序输出，若结果超过32bit，则输出0

我遇到的主要问题在于对结果是否溢出的判定上，一开始采用的方式是'res>0xffffffff?'，现在看来int显然是个有符号的数据类型，或许0xffffffff/2可行？
后来发现了一个<climits>头文件，INT_MAX,INT_MIN直接解决了这个问题。

然后是算法，其实逆序输出从一开始学C的时候就做过，不过这一题中采用的思路还是挺巧妙的，感觉像是取输入的最后一位然后不断左移？
